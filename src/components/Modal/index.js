import React from 'react'
import "./index.css"

function Modal() {
    return(
        <>
            {/* <div className="modal">
                <span className="btClose">X</span>
                <div className="imgContainer">
                    <img src={miras[0].src}/>
                </div>
                <h2>{miras[0].nome}</h2>
                <div className="aimConfigs">
                    <ul className="aimList">
                        <li>Internas</li>
                        {miras[0].internas.map(mira=>
                            <li>{mira}</li>
                        )}
                    </ul>
                    <ul className="aimList">
                        <li>Externas</li>
                        {miras[0].externas.map(mira=>
                            <li>{mira}</li>
                        )}
                    </ul>
                </div>
            </div>
         */}
        </>
    )
}

export default Modal